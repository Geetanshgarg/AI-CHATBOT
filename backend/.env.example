# Backend environment variables for AI-CHATBOT
# Copy this file to .env and fill the real values (do NOT commit .env containing secrets)

# MongoDB connection string
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.example.mongodb.net/your-db?retryWrites=true&w=majority

# Server port (optional, defaults to 5000 if omitted)
PORT=5000

# Secret used by cookie-parser for signing cookies (strong random string)
COOKIE_SECRET=replace_with_a_strong_random_string

# JWT secret used to sign/verify JWT tokens (strong random string)
JWT_SECRET=replace_with_a_strong_random_string

# Domain used when setting cookies; for local development you can set this to localhost
# For production use the top-level domain like .example.com
DOMAIN=localhost

# Google Gemini AI configuration
# Your Google Gemini API key (get it from https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key_here
